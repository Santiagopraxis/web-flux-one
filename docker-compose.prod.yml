services:
  web-flux:
    image: ghcr.io/santiagopraxis/web-flux-one:latest
    build:
      context: ./flux-web-final
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - flux-net

networks:
  flux-net:
    external: true
